# 9. Danh sách công việc để hoàn thiện hệ thống theo chuẩn tài liệu End-to-End (1-8)

Tài liệu này tổng hợp toàn bộ gap giữa **business flow chuẩn** (đã viết lại ở file 1-8) và **code hiện tại**, xếp theo mức độ ưu tiên/rủi ro. Dùng làm backlog cho team.

## Cách đọc bảng
- **Trạng thái**: Đã có / Thiếu / Cần rà soát
- **Rủi ro nếu không làm**: hậu quả nghiệp vụ thực tế
- **Việc cần làm**: đề xuất kỹ thuật cụ thể

---

## Ưu tiên CAO (rủi ro compliance / data integrity — ảnh hưởng trực tiếp tới tính pháp lý hồ sơ)

### 1. Bulk-verify Evidence — ✅ Đã triển khai (2026-08-08)
- **Module liên quan**: 6 — Assessment & Evidence
- **Trạng thái**: Đã có. Chi tiết implementation + hướng dẫn sử dụng cho team: `docs/maintain/2026-08-08_feature-bulk-verify-evidence.md`.
- **Tóm tắt đã làm**:
  - `PUT /api/evidences/bulk-verify` (QA, Admin) — nhận `List<int> EvidenceIds` + 1 `VerificationStatus`/`VerificationComment` chung cho cả batch.
  - Mỗi item xử lý độc lập: 1 item lỗi (không tồn tại, tự verify minh chứng của chính mình) KHÔNG rollback các item còn lại — response tách rõ `Verified`/`Failed` kèm lý do.
  - Ghi 1 `AuditLog` riêng cho **từng** EvidenceId được verify (không gộp 1 log cho cả batch) — đúng yêu cầu ban đầu.
  - Tác dụng phụ tích cực: refactor gộp logic dùng chung giữa single-verify và bulk-verify khiến `VerifyEvidenceAsync` (single) từ chỗ KHÔNG có AuditLog trước đây, giờ cũng được ghi Audit đầy đủ.
- **Quyết định nghiệp vụ đã chốt (họp team 2026-08-08, xem `docs/todo/addition.md`)**: **QA KHÔNG cần giới hạn theo phạm vi lớp/khóa** — QA được phép quản lý/verify toàn bộ Evidence trong hệ thống, không scope theo Department/Course/Class. Lý do team đưa ra: Audit Trail đã ghi đầy đủ ai là người verify từng Evidence, nên không cần chia nhỏ phạm vi truy cập của QA để kiểm soát trách nhiệm — việc truy vết trách nhiệm nằm ở tầng Audit, không phải tầng phân quyền truy cập. **Không cần code thêm gì cho câu hỏi này** — giữ nguyên hành vi hiện tại (bất kỳ QA nào cũng verify được bất kỳ Evidence nào).

### 2. Amendment / Unlock-request ở cấp SubjectSignoff — ✅ Đã triển khai + sửa theo quyết định họp (2026-08-08)
- **Module liên quan**: 6, 4 — Assessment & Evidence, ETR Management
- **Trạng thái**: Đã có, đã sửa đúng theo quyết định họp team. Chi tiết implementation gốc: `docs/maintain/2026-08-08_feature-amendment-unlock-request-subjectsignoff.md`; chi tiết fix RBAC theo họp team: `docs/maintain/2026-08-08_fix-amendment-unlock-request-identity-check-and-rbac.md`.
- **Tóm tắt đã làm (bản gốc)**:
  - Entity mới `AmendmentRequest`: `SubjectResultId, RequestedByAccountId, Reason, OldValue, NewValue, Status (Pending/Approved/Rejected), ApprovedByAccountId, ApprovedAt, DecisionComment` (migration `AddAmendmentRequest`).
  - `POST /api/amendments/{id}/approve` và `POST /api/amendments/{id}/reject` (TrainingManager/Admin duyệt) — Approve reset `SubjectResult.Status="Pending"` + soft-delete toàn bộ SubjectSignoff cũ (buộc ký lại thật); Reject bắt buộc có Comment, không đổi gì.
  - Chặn tạo/Approve nếu ETR cha đã `Completed`/`IsLocked` (kiểm tra 2 lần — lúc tạo VÀ lúc Approve) — hướng dẫn dùng `POST /etr/{id}/reopen` thay thế, tránh 2 cơ chế mở khóa chồng chéo.
- **Tóm tắt đã làm (fix theo quyết định họp team 2026-08-08, `docs/todo/addition.md`)**:
  1. ✅ **Identity check cho Instructor**: `AmendmentService.CreateAmendmentRequestAsync` giờ kiểm tra `SubjectSignoff.SignoffByAccountId == requestedByAccountId` — Instructor không khớp → 403 Forbidden, không tạo record, không ghi audit.
  2. ✅ **Loại Academic khỏi quyền gọi unlock-request**: `SubjectSignoffController.RequestUnlock` giờ có `[Authorize(Roles = "Instructor,Admin")]` riêng (thu hẹp so với class-level `Instructor,Academic,Admin` — 2 attribute kết hợp theo AND) — chỉ còn `Instructor, Admin` gọi được.
  3. ✅ **Admin Force Unlock có Audit Log riêng**: khi Admin gọi cho Subject không phải do chính họ ký, ghi `AuditLog` với `ActionType = "ADMIN_FORCE_UNLOCK"` (khác `"AMENDMENT_REQUEST"` bình thường), mô tả rõ `[CẢNH BÁO]` kèm AccountId của cả Admin và người ký gốc.
- **Còn lại (không thuộc scope task này, đã rà soát kỹ — xem mục 4 trong `docs/maintain/2026-08-08_fix-amendment-unlock-request-identity-check-and-rbac.md`)**:
  - **SQL deploy thủ công**: rà soát lại phát hiện `Deploy_ETR_System.sql`, `ALL_IN_ONE_Deploy.sql`, `Deploy_NukeAndSeed.sql` KHÔNG chỉ thiếu bảng `AmendmentRequests` mà đang lạc hậu tới **9 migration** (dừng ở `2026-07-19`, thực tế đã tới `2026-08-08`). **Quyết định: KHÔNG vá lẻ 1 bảng vào file đã lạc hậu** (sẽ tạo cảm giác sai là "đã cập nhật" trong khi vẫn thiếu rất nhiều cột/bảng khác) — khuyến nghị team dùng `dotnet ef migrations script --idempotent` để sinh lại toàn bộ script thay vì tiếp tục bảo trì tay 3 file này.

### 3. Versioning cho Course & CompletionRequirement — ✅ Đã triển khai (2026-08-08)
- **Module liên quan**: 3, 7 — Course/Class/Enrollment, Completion Requirement
- **Trạng thái**: Đã có. Chi tiết implementation + hướng dẫn sử dụng cho team: `docs/maintain/2026-08-08_feature-course-completionrequirement-versioning.md`.
- **Tóm tắt đã làm**:
  - Thêm `VersionNo`, `EffectiveFrom` vào `Course`; `VersionNo`, `EffectiveFrom`, `EffectiveTo` vào `CompletionRequirement`; `CourseVersionNo` vào `ETRCourseRecord` (migration `AddCourseAndCompletionRequirementVersioning`).
  - `Course.VersionNo` bump khi `ValidityMonths` đổi (trường Course duy nhất có logic đọc ảnh hưởng kết quả).
  - `PUT /api/completionrequirements/{id}` khi đổi `RequirementType`/`ThresholdValue`/`IsMandatory`: đóng row cũ (`EffectiveTo`), tạo row MỚI với `VersionNo` tăng — **không ghi đè**. Đổi field mô tả (Name/Description/DisplayOrder) vẫn ghi đè tại chỗ như cũ.
  - `EnrollmentService.CreateEnrollmentAsync` snapshot `Course.VersionNo` vào `ETRCourseRecord.CourseVersionNo` tại thời điểm Enroll.
  - `EtrService.SubmitEtrAsync`/`GetCompletionProgressAsync` lọc `CompletionRequirement` theo đúng `CourseVersionNo` đã snapshot, không theo bản mới nhất — học viên cũ không bao giờ bị đối chiếu nhầm theo luật mới ban hành sau khi họ đã enroll.
  - `GET /completionrequirements/course/{courseId}` chỉ trả bản đang hiệu lực (`EffectiveTo == null`); lịch sử đầy đủ vẫn xem qua `GET /completionrequirements` hoặc `GET /completionrequirements/{id}`.
- **Còn lại (không thuộc scope task này)**:
  - Chưa đồng bộ các cột mới vào 3 file raw SQL deploy thủ công (`Deploy_ETR_System.sql`, `ALL_IN_ONE_Deploy.sql`, `Deploy_NukeAndSeed.sql`) — cùng loại thiếu sót đã ghi nhận ở mục #2, không vá lẻ (xem lý do ở mục #2), khuyến nghị regenerate bằng `dotnet ef migrations script --idempotent`.
  - `PUT /completionrequirements/{id}` có thể trả về `RequirementId` KHÁC id trong URL khi đổi field ảnh hưởng kết quả (hành vi có chủ đích, đã ghi rõ trong doc comment controller) — FE cần cập nhật để đọc id mới từ response.

- **✅ Đã xử lý bổ sung (2026-08-08)**: `CourseSubject.PassingScore`, `Assessment.PassingScore` và `Assessment.Weight` — chi tiết: `docs/maintain/2026-08-08_feature-coursesubject-assessment-passingscore-snapshot.md`.
  - Rà soát xác nhận đây là rủi ro THẬT (không suy đoán): `AssessmentResultService.RecordAssessmentScoreAsync`/`EvaluateSubjectPassabilityAsync`/`CalculateSubjectResultScoreAsync` đọc live `PassingScore`/`Weight` mỗi lần chấm điểm/đánh giá lại/tính điểm trung bình.
  - **Không dùng được pattern append-only-row như CompletionRequirement** vì `CourseSubject` có composite Primary Key `(CourseId, SubjectId)`, không có surrogate ID — đổi PK là quyết định thiết kế lớn hơn, không tự ý làm.
  - **Giải pháp thay thế: Snapshot tại thời điểm phát sinh dữ liệu** — thêm `PassingScoreSnapshot`/`WeightSnapshot` (nullable) vào `SubjectResult`/`AssessmentResult`, chụp giá trị đang áp dụng ngay tại Enroll/lần chấm điểm đầu tiên, dùng snapshot đó cho mọi lần chấm/đánh giá lại/tính điểm trung bình sau này (kể cả khi Instructor re-sign-off sau Amendment) thay vì đọc lại giá trị live.
  - Migration `AddPassingScoreSnapshot`, `AddWeightSnapshot`; cột nullable nên dữ liệu cũ tự fallback về giá trị live, không breaking. **Không còn điểm nào tồn đọng của mục này.**

> ⚠️ **2 điểm cần team lưu ý** (chi tiết trong maintain doc mục 4, `docs/maintain/2026-08-08_feature-course-completionrequirement-versioning.md` và `docs/maintain/2026-08-08_feature-coursesubject-assessment-passingscore-snapshot.md`):
> 1. **`PUT /api/completionrequirements/{id}` có thể đổi ID** — khi request thay đổi một field ảnh hưởng kết quả (`RequirementType`/`ThresholdValue`/`IsMandatory`), endpoint tạo bản ghi MỚI thay vì ghi đè, nên response trả về `RequirementId` KHÁC với id trong URL. FE cần đọc ID từ response cho các lần gọi tiếp theo, không được giữ ID cũ.
> 2. **Chưa đồng bộ SQL deploy thủ công** — các cột mới (`VersionNo`, `EffectiveFrom`, `EffectiveTo`, `CourseVersionNo`, `PassingScoreSnapshot`, `WeightSnapshot`) chỉ có trong migration EF Core. Khuyến nghị dùng `dotnet ef migrations script --idempotent` để sinh lại toàn bộ thay vì vá tay 3 file đã lạc hậu ~10 migration.
>
> (`CourseSubject`/`Assessment` dùng mô hình Snapshot thay vì Versioning row đầy đủ như CompletionRequirement, do ràng buộc composite PK — lịch sử row-level cấp bảng gốc chỉ có thể suy ra gián tiếp qua `AssessmentResult.PassingScoreSnapshot`/`WeightSnapshot` đã lưu theo từng học viên, không phải hạn chế cần xử lý thêm.)

### 11. Data Isolation cho Instructor — "sân nhà ai nấy đá" — ✅ Đã triển khai (2026-08-08)
- **Module liên quan**: 3, 5, 6 — Course/Class/Enrollment, Attendance, Assessment & Evidence
- **Trạng thái**: Đã có. Chi tiết implementation + hướng dẫn sử dụng cho team: `docs/maintain/2026-08-08_feature-instructor-class-data-isolation.md`.
- **Tóm tắt đã làm**:
  - `ClassOwnershipValidator` (`ETR.Application/Compliance/`) — helper dùng chung, chỉ áp dụng cho role Instructor, không giới hạn Admin/Academic/TrainingManager/QA/Audit.
  - `ClassService.GetAllClassesAsync` — Instructor chỉ thấy lớp có `InstructorAccountId` khớp chính mình; Admin/role khác vẫn thấy toàn bộ.
  - `AttendanceService.RecordAttendanceAsync`, `AssessmentResultService.RecordAssessmentScoreAsync`, `AssessmentResultService.SignoffSubjectResultAsync`, `EvidenceService.UploadEvidenceAsync` — cả 4 đều resolve ra đúng `Class` của hành động (qua Session/Enrollment/SubjectResult chain) rồi validate ownership — Instructor không được phân công → 403 Forbidden.
  - `EvidenceService.UploadEvidenceAsync` check ownership TRƯỚC khi ghi file vào đĩa, tránh để lại file rác khi request bị từ chối.
- **Còn lại (không thuộc scope task này)**:
  - Chưa rà soát cùng loại check cho `SessionsController` (tạo/sửa/xóa/confirm Session) và các API "xem" khác ngoài `GET /api/classes` (VD: `GET /api/classstudents`, `GET /api/attendance`) — todo doc gốc chỉ liệt kê đích danh 5 điểm chạm đã xử lý.
  - **Ngoại lệ dạy thay/chấm chéo (nhiều Instructor/1 Class) — ĐÃ CHỐT HOÃN LẠI theo quyết định họp team 2026-08-08**: giữ nguyên mô hình `Class.InstructorAccountId` dạng single-value; KHÔNG tự ý mở rộng thành bảng trung gian (`ClassInstructors_Mapping`) nếu không có yêu cầu nghiệp vụ cụ thể — tránh over-engineering cho use-case chưa ai cần ngay.

> ⚠️ **1 điểm chưa làm, cần team xem xét lại sau** (đã ghi trong maintain doc mục 4, `docs/maintain/2026-08-08_feature-instructor-class-data-isolation.md`): việc quản lý buổi học (tạo/sửa/xóa/xác nhận Session) và các API "xem" khác (VD: danh sách điểm danh) chưa được rà soát cùng loại kiểm tra Data Isolation này — todo doc gốc chỉ nêu đích danh 5 điểm đã xử lý (`GetAllClassesAsync`, `RecordAttendanceAsync`, `RecordAssessmentScoreAsync`, `SignoffSubjectResultAsync`, `UploadEvidenceAsync`). Nếu phát hiện rủi ro tương tự ở các API khác, cần một lượt rà soát riêng.

### 4. Certificate Validity, Grace Period, Grounded status, Recurrent Training — ✅ Đã triển khai (2026-08-07, sửa lại cùng ngày)
- **Module liên quan**: 4 — ETR Management
- **Trạng thái**: Đã có, đã sửa đúng theo xác nhận nghiệp vụ. Chi tiết implementation + hướng dẫn sử dụng cho team: `docs/maintain/2026-08-07_feature-certificate-validity-grounded-status-recurrent-training.md`.
- **Tóm tắt đã làm**:
  - `LearnerStatus.Grounded` (hằng số, `ETR.Application/Compliance/LearnerStatus.cs`) — giá trị hợp lệ mới cho `UserProfile.Status`.
  - `CertificateValidityCalculator.HasAnyExpiredCompletedEtrAsync` (`ETR.Application/Compliance/`) — logic chung xác định "học viên có course nào đang hết hạn chứng chỉ hay không", dùng lại ở cả 2 nơi dưới đây.
  - `POST /api/etr/refresh-grounded-status` (Admin) — quét toàn bộ UserProfile Active/Grounded, tự Grounded/clear-Grounded, ghi AuditLog đầy đủ. Đây là trigger thủ công (chưa có background job/cron trong scope).
  - `GET /api/etr/due-for-training?courseId=&daysThreshold=30` — mở rộng từ `expiring-students`, `courseId` optional (bỏ trống = quét toàn bộ Course).
  - `EtrService.CompleteEtrAsync` — tự động clear Grounded **chỉ khi ETR mới đó thực sự `Completed`** (Training Manager Approve), không phải ngay khi Enroll lại — **đã sửa lại từ vị trí ban đầu** (`EnrollmentService.CreateEnrollmentAsync`) sau khi xác nhận về an toàn bay: Enroll lại (chưa hoàn thành khóa) không được coi là "đủ điều kiện làm nhiệm vụ".
- **Còn lại (không thuộc scope task này)**:
  - Chưa có background job/cron tự động gọi `refresh-grounded-status` định kỳ — hiện phải trigger thủ công hoặc qua cron ngoài hệ thống.

---

## Ưu tiên THẤP (rõ ràng hóa, không phải thiếu nghiệp vụ nhưng gây hiểu nhầm/khó bảo trì)

### 5. Đặt lại tên route Withdraw Enrollment
- **Module liên quan**: 3
- **Trạng thái**: Logic đã đúng (`DELETE /enrollments/{id}` thực chất là soft-withdraw, có rollback ETR), nhưng route name gây hiểu nhầm là xóa cứng.
- **Việc cần làm**: Thêm route `POST /enrollments/{id}/withdraw` làm alias rõ nghĩa, giữ `DELETE` cho trường hợp Admin hard-delete thật (nếu có nhu cầu), tách rõ 2 semantics.

### 6. Chuẩn hóa ActionType cho ApprovalsController.process
- **Module liên quan**: 7
- **Trạng thái**: Cần rà soát — hiện dùng 1 endpoint chung `POST /approvals/{id}/process` cho nhiều hành động (Verify/Return/Reject/Approve), dựa vào tham số `ActionType`.
- **Việc cần làm**: Định nghĩa enum `ApprovalActionType` dùng chung FE-BE, đảm bảo tầng service validate quyền theo từng ActionType cụ thể (không chỉ dựa vào Role chung của account).

### 7. Rà soát sửa Attendance sau khi Session confirm — ✅ Đã có sẵn (xác nhận lại 2026-08-07)
- **Module liên quan**: 5
- **Trạng thái**: Đã chặn đúng. `AttendanceService.UpdateAttendanceRecordAsync`/`DeleteAttendanceRecordAsync` (`ETR.Application/Services/AttendanceService.cs:136,173`) throw `BusinessRuleViolationException` khi `Session.IsConfirmed=true`. Mục này trước đây ghi "cần rà soát" — đã rà soát xong, không cần làm thêm, chỉ cần đính chính trạng thái trong doc này.

### 8. Giới hạn số lần Retake (MaxAttempt) — ✅ Đã có sẵn (xác nhận lại 2026-08-07)
- **Module liên quan**: 6
- **Trạng thái**: Đã có. `BusinessRuleEngine.MaxAssessmentAttempts = 3` (`ETR.Application/Compliance/BusinessRuleEngine.cs:8`) được validate trong `AssessmentResultService.cs:146-148` — throw `BusinessRuleViolationException` khi `attemptNo > MaxAssessmentAttempts`. Mục này trước đây ghi "cần rà soát" — đã rà soát xong, không cần làm thêm, chỉ cần đính chính trạng thái trong doc này.

### 9. Chặn `GET /auth/mock-admin-token` ở production
- **Module liên quan**: 1
- **Trạng thái**: Rủi ro bảo mật nếu vô tình deploy nguyên trạng.
- **Việc cần làm**: Environment guard (chỉ available khi `ASPNETCORE_ENVIRONMENT=Development`), hoặc xóa hoàn toàn trước khi release.

### 10. Entity trùng lặp: `CourseEnrollment` vs `ClassStudent`
- **Module liên quan**: 3 — Course/Class/Enrollment (và gián tiếp 5, 6 vì phụ thuộc FK)
- **Trạng thái**: Xác nhận trùng lặp thật trong code — không phải hiểu nhầm đọc docs.
- **Chi tiết kỹ thuật**: Mỗi lần `POST /enrollments` thành công, `EnrollmentService.EnrollAsync` (dòng ~135-171) tạo **đồng thời 2 bản ghi** với dữ liệu lặp 100%:
  - `CourseEnrollment { AccountId, ClassId, Status, EnrolledAt, StartDate, ExpectedCompletionDate, ActualCompletionDate }`
  - `ClassStudent { CourseEnrollmentId, ClassId, AccountId, Status }` — 3/4 field là bản sao trực tiếp của `CourseEnrollment`, không chứa thông tin nghiệp vụ mới nào.
  - Khi Enrollment bị sửa/withdraw, code phải tự tay đồng bộ lại `ClassStudent` (`EnrollmentService.cs` dòng ~268-279, comment "Đồng bộ ClassStudent") — 2 nguồn sự thật (source of truth) cho cùng 1 dữ liệu, không được DB enforce, chỉ dựa vào dev nhớ sync đúng chỗ.
  - Lý do tồn tại `ClassStudent`: dùng làm điểm neo FK cho `AttendanceRecord.ClassStudentId` và `AssessmentResult`/Evidence liên quan, thay vì trỏ trực tiếp `EnrollmentId`.
- **Rủi ro nếu không làm**: Mỗi lần sửa logic Enrollment trong tương lai (VD: bổ sung mục #5 đổi tên route withdraw, hoặc mục #4 Recurrent Training) dev có nguy cơ quên đồng bộ `ClassStudent`, dẫn đến Attendance/Assessment của học viên trỏ vào 1 `ClassStudent` có Status khác với `CourseEnrollment` thật (VD: Enrollment đã Withdrawn nhưng ClassStudent vẫn Active) → dữ liệu điểm danh/điểm số ma, sai lệch khi Auditor tra cứu.
- **Việc cần làm (đề xuất, cần review kỹ trước khi thực hiện vì đụng nhiều bảng)**:
  1. Rà soát toàn bộ nơi dùng `ClassStudentId` (Repository, DTO, FE nếu có) để xác định phạm vi ảnh hưởng đầy đủ trước khi đổi.
  2. Phương án khuyến nghị: bỏ bảng `ClassStudent`, đổi `AttendanceRecord.ClassStudentId` và các FK liên quan trỏ trực tiếp `CourseEnrollment.EnrollmentId` — giảm 1 tầng gián tiếp, xóa hẳn nhu cầu đồng bộ tay.
  3. Nếu có lý do giữ tách bảng (VD: dự tính hỗ trợ 1 Enrollment ánh xạ nhiều Class trong tương lai — hiện tại KHÔNG đúng vì `CourseEnrollment` đã có `ClassId` riêng, nên trường hợp này chưa xảy ra), cần ghi rõ lý do thiết kế vào code comment để tránh hiểu nhầm là duplicate ở các lần review sau.
  4. Viết migration EF Core + cập nhật toàn bộ Repository/Service liên quan, kèm test hồi quy cho Attendance/Assessment/Evidence trước khi merge.

---

## Tổng kết theo module (checklist nhanh cho team lead)

| Module | Việc cần làm còn tồn đọng |
|---|---|
| 1. User Management | #9 chặn mock-admin-token |
| 2. Learner Profile | ✅ #4 Grounded status (đã triển khai) |
| 3. Course/Class/Enrollment | ✅ #3 Versioning (đã triển khai), #5 đổi tên route withdraw, ✅ #11 Data Isolation Instructor (đã triển khai) |
| 4. ETR Management | ✅ #2 Amendment cấp Subject (đã triển khai + sửa RBAC theo họp), ✅ #4 Grounded/Recurrent Training (đã triển khai, xem `docs/maintain/2026-08-07_...`) |
| 5. Attendance | ✅ #7 đã chặn sửa sau confirm (đính chính, không cần làm thêm), ✅ #11 Data Isolation Instructor (đã triển khai) |
| 6. Assessment & Evidence | ✅ #1 bulk-verify (đã triển khai, QA-scoping đã chốt KHÔNG cần làm), ✅ #2 Amendment (đã triển khai + sửa RBAC theo họp), ✅ #8 giới hạn Retake (đã có sẵn), ✅ #11 Data Isolation Instructor (đã triển khai) |
| 7. Completion & Approval | ✅ #3 Versioning ảnh hưởng Checklist (đã triển khai), #6 chuẩn hóa ActionType |
| 8. Reporting/Search/Audit | Không có gap kỹ thuật riêng — phụ thuộc dữ liệu sạch từ #2, #3, #4 |
| 10. (cross-cutting) Entity trùng lặp | #10 `CourseEnrollment` vs `ClassStudent` — cần review trước khi migrate |

**Còn tồn đọng thật (chưa triển khai) sau lượt rà soát 2026-08-08 (bổ sung quyết định họp team — `docs/todo/addition.md`):** #5 (đổi tên route withdraw), #6 (chuẩn hóa ActionType Approvals), #9 (chặn mock-admin-token ở production), #10 (entity trùng lặp CourseEnrollment/ClassStudent).
**Đã triển khai/đã xác nhận đúng, không cần làm thêm:** #1 (Bulk-verify Evidence — 2026-08-08; QA-scoping đã CHỐT không cần làm theo quyết định họp team), #2 (Amendment/Unlock-request — 2026-08-08, đã sửa đủ 3 điểm RBAC theo họp team; riêng SQL deploy thủ công KHÔNG vá lẻ, xem lý do trong mục #2), #3 (Versioning Course/CompletionRequirement — 2026-08-08, riêng CourseSubject/Assessment vẫn chưa version hóa và sync SQL deploy thủ công vẫn chưa làm — xem #3), #4 (Certificate Validity/Grounded/Recurrent Training — 2026-08-07), #7 (chặn sửa Attendance sau confirm — đã có sẵn từ trước, chỉ đính chính trạng thái doc), #8 (giới hạn Retake — đã có sẵn từ trước, chỉ đính chính trạng thái doc), #11 (Data Isolation cho Instructor — 2026-08-08, riêng SessionsController và các API "xem" khác chưa được rà soát cùng loại check — xem #11).
